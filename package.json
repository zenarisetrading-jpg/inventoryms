{
  "name": "inventory-planner-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --prefix frontend",
    "install-frontend": "npm install --prefix frontend",
    "start": "serve -s frontend/dist -l $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "supabase": "^2.84.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
